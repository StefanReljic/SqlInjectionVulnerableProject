import * as type from '../../../application_styles'

export const addButton = {
    ...type.addButton,
    marginLeft: "15px"
}

export const cancelButton = {
    ...type.cancelButton,
    marginLeft: "15px"
}
